import * as PublishActionsStories from './publish-actions.stories';
import { Meta, Canvas } from '@storybook/blocks'

<Meta of={PublishActionsStories} />

> Publish actions is used in the published charts to
> allow users to share and embed charts in other websites.

<Canvas of={PublishActionsStories.PublishActions} />
